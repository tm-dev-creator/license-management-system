{
  "name": "subscription-management-api",
  "version": "1.0.0",
  "description": "Subscription management API with frontend (JWT) and SDK (API key) auth",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "init-db": "node db/init.js",
    "seed": "node db/seed.js",
    "setup": "node db/init.js && node db/seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.6.0",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^10.0.0"
  }
}
